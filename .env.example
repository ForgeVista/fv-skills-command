# FV Skills Command — environment variables
# Copy to .env and fill in values (never commit .env)

# Optional: override default autogit commit interval (seconds)
# AUTOGIT_INTERVAL=300

# Optional: override default skills folder on launch
# DEFAULT_SKILLS_PATH=/path/to/your/skills
